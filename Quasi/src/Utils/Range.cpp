#include "Range.h"

namespace Quasi {
    template struct Range<int>;
    template struct Range<uint>;
    template struct Range<usize>;
    template struct Range<float>;
    template struct Range<double>;
}